// Once you've written your test code in `archive-tasks.test.ts`, then update your function from
// ex01 so that it passes. Since we have at least three examples, it's okay to write a solution
// that gets the job done for more general cases.
