// This is where we will write the code to make our test pass. Please write the test first,
// rather than jumping in and writing a solution. The plan is to take a TDD approach where we write
// _only_ enough code to make the test pass.

import {TasksState} from '../../types';

const moveOldTasksToArchive = (tasks: TasksState, start: number) => ({
    // Your solution goes here. Write only enough code to make the test pass. No more.
});

export default moveOldTasksToArchive;
